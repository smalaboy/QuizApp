<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tech.smal.turkaf">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FirebaseHandler"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".HomeScreenActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".FactoryActivity" />
        <activity android:name=".NewQuestionActivity" />
        <activity android:name=".PropositionsActivity" />
        <activity android:name=".QuestionDetailsActivity" />
        <activity android:name=".RegistrationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".ProfileActivity"
            android:label="@string/title_activity_profile"
            android:parentActivityName=".HomeScreenActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.tech.smal.turkaf.HomeScreenActivity" />
        </activity>
        <activity
            android:name=".TestActivity"
            android:label="@string/title_activity_test" />
        <activity android:name=".HomeActivity" />

        <meta-data
            android:name="com.google.gms.ads.AD_MANAGER_APP"
            android:value="true" />

        <activity android:name=".ResultsActivity"></activity>
    </application>

</manifest>